# Deep validation suite

This directory contains integration tests and synthetic fixtures used to prove BrainForgeMD against real file containers and hostile-input cases without relying on private data.

The suite is intentionally synthetic and reproducible. It exercises document creation, conversion, provenance, chunking, GraphRAG exports, incremental behaviour, archive protections, and optional rich-format backends.
